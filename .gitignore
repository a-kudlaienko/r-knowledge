__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
*.env
# Per-project storage config — may name a remote PG host / topology; keep it
# out of the published repo so a contributor can't commit theirs by accident.
.knowledge-config.json
# Local index databases — never publish a built index.
*.sqlite
*.sqlite3
.venv/
venv/
.pytest_cache/
.coverage
.mypy_cache/
.ruff_cache/
.DS_Store
.claude/
tasks/
tests/
.idea/
.aider.*
uv.lock
*relations_graph.html
CLAUDE.local.md
AGENTS.local.md
relations_graph.html
